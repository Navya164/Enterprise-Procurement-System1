package com.pms.entity;

/**
 * PLACEHOLDER - DELETE if you already have a PurchaseRequestStatus enum
 * in your existing Purchase Request module. Included only so this module
 * compiles and runs standalone for testing.
 */
public enum PurchaseRequestStatus {
    PENDING,
    APPROVED,
    REJECTED
}
